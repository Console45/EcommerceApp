import React, { FC, ReactElement } from "react";
import { View, StyleSheet } from "react-native";

interface ProductDetailProps {}

export const ProductDetail: FC<ProductDetailProps> = ({}): ReactElement => {
  return <View></View>;
};

const styles = StyleSheet.create({});
